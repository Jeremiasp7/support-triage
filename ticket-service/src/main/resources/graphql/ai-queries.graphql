mutation AnalyzeTicket($input: TicketInput!) {
    analyzeTicket(input: $input) {
        suggestedCategory
        suggestedPriority
        proposedSolution
        confidence
        requiresHuman
        sources {
            documentTitle
            excerpt
        }
    }
}